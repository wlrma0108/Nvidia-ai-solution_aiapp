version: '3.8'

services:
  api:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
    environment:
      - PADDLE_HOME=/app/.paddle_cache
      - HOME=/app
    restart: unless-stopped
